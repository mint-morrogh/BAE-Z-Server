hostname = "Mint Mundane Server";	// Server name
password = "mintmorrogh";			// Server password - change this to your own
passwordAdmin = "mintmorrogh";		// Admin password - type #login <password> in chat for admin access

enableWhitelist = 0;		// Enable/disable whitelist (value 0-1)
enableCfgGameplayFile = 1;	// Enable cfggameplay.json gameplay settings

maxPlayers = 4;				// Just you and your wife, with a little room for friends

verifySignatures = 2;		// Verifies .pbos against .bisign files. (only 2 is supported)

forceSameBuild = 1;			// When enabled, the server will allow the connection only to clients with same the .exe revision as the server (value 0-1)

disableVoN = 0;				// Enable/disable voice over network (value 0-1)
vonCodecQuality = 20;		// Voice over network codec quality, the higher the better (values 0-30)

disable3rdPerson = 0;		// Toggles the 3rd person view for players (value 0-1)
disableCrosshair = 0;		// Toggles the cross-hair (value 0-1)

serverTime = "2000/07/01/08/00";	// Start at 8 AM so you get a full day of light
serverTimeAcceleration = 6;	// 6x speed: a full 24h cycle takes 4 real hours. Daytime lasts ~2.5 hours, night ~1.5 hours.
serverNightTimeAcceleration = 4;// Nights pass 4x faster ON TOP of the 6x (effectively 24x). Night lasts about ~22 minutes.
serverTimePersistent = 1;	// Saves the time between restarts so you don't always start at 8 AM

guaranteedUpdates = 1;		// Communication protocol used with game server (use only number 1)

loginQueueConcurrentPlayers = 5;	// The number of players concurrently processed during the login process. Should prevent massive performance drop during connection when a lot of people are connecting at the same time.
loginQueueMaxPlayers = 500;			// The maximum number of players that can wait in login queue

instanceId = 1;				// DayZ server instance id, to identify the number of instances per box and their storage folders with persistence files

storageAutoFix = 1;			// Checks if the persistence files are corrupted and replaces corrupted ones with empty ones (value 0-1)


class Missions
{
	class DayZ
	{
		template = "dayzOffline.chernarusplus"; // Mission to load on server startup. <MissionName>.<TerrainName>
	};
};